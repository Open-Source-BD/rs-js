/target
/node_modules
/coverage
/jest.config.js
.DS_Store
.vscode
.idea

# pkg-node/, pkg/, pkg-web/ are intentionally NOT gitignored.
# Commit them so npm consumers get pre-built WASM without needing Rust.
# Rebuild with: npm run build
